language: go

go:
  - "1.x"

addons:
  chrome: stable

before_install:
  - nvm install 10
  - nvm use 10
  - make deps
  - google-chrome-stable --headless --disable-gpu --remote-debugging-port=9222 http://localhost &

script:
  - make all